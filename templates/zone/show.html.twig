{% extends 'base.html.twig' %}

{% block title %}Zone{% endblock %}

{% block body %}
    <h1>Zone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zone.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ zone.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ zone.description }}</td>
            </tr>
            <tr>
                <th>Width</th>
                <td>{{ zone.width }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ zone.height }}</td>
            </tr>
            <tr>
                <th>Delivery</th>
                <td>{{ zone.delivery }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ zone.type }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ zone.category }}</td>
            </tr>
            <tr>
                <th>Ad_selection</th>
                <td>{{ zone.adSelection }}</td>
            </tr>
            <tr>
                <th>Chain</th>
                <td>{{ zone.chain }}</td>
            </tr>
            <tr>
                <th>Prepend</th>
                <td>{{ zone.prepend }}</td>
            </tr>
            <tr>
                <th>Append</th>
                <td>{{ zone.append }}</td>
            </tr>
            <tr>
                <th>AppendType</th>
                <td>{{ zone.appendType }}</td>
            </tr>
            <tr>
                <th>ForceAppend</th>
                <td>{{ zone.forceAppend ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>InventoryForecastType</th>
                <td>{{ zone.inventoryForecastType }}</td>
            </tr>
            <tr>
                <th>Comments</th>
                <td>{{ zone.comments }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ zone.cost }}</td>
            </tr>
            <tr>
                <th>CostType</th>
                <td>{{ zone.costType }}</td>
            </tr>
            <tr>
                <th>CostVariableId</th>
                <td>{{ zone.costVariableId }}</td>
            </tr>
            <tr>
                <th>TechnologyCost</th>
                <td>{{ zone.technologyCost }}</td>
            </tr>
            <tr>
                <th>TechnologyCostType</th>
                <td>{{ zone.technologyCostType }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ zone.createdAt ? zone.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ zone.updatedAt ? zone.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Block</th>
                <td>{{ zone.block }}</td>
            </tr>
            <tr>
                <th>Capping</th>
                <td>{{ zone.capping }}</td>
            </tr>
            <tr>
                <th>SessionCapping</th>
                <td>{{ zone.sessionCapping }}</td>
            </tr>
            <tr>
                <th>What</th>
                <td>{{ zone.what }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ zone.rate }}</td>
            </tr>
            <tr>
                <th>Pricing</th>
                <td>{{ zone.pricing }}</td>
            </tr>
            <tr>
                <th>OacCategoryId</th>
                <td>{{ zone.oacCategoryId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zone_index') }}">back to list</a>

    <a href="{{ path('app_zone_edit', {'id': zone.id}) }}">edit</a>

    {{ include('zone/_delete_form.html.twig') }}
{% endblock %}
